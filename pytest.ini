[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers --basetemp=.tmp_pytest
markers =
    article: article mode checks
    card_news: card-news mode checks
    integration: integration coverage around lint entrypoints
    slow: slower smoke coverage
