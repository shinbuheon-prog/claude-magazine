# TASK_046 Smoke Report

Date: 2026-04-24

Commands:

```bash
python -m py_compile pipeline/figma_client.py pipeline/figma_card_news_sync.py
python pipeline/figma_card_news_sync.py --slides-json reports/task041_smoke_1.json --file-key demo-file --dry-run --paste-package
```

Checks:

- Figma sync plan JSON generated under `output/figma_sync/`
- Paste package generated with per-slide Markdown files plus `README.md`
- Layout fallback metadata preserved in plan output
- No MCP dependency required for the dry-run path

Limitations:

- Live Figma REST metadata and image export were not exercised because no real token was provided in this environment.
