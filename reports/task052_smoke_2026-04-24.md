# TASK_052 Smoke Report

- Date: 2026-04-24
- Scope: CI workflow, PR template, CI usage guide, local smoke commands that mirror CI jobs

## Commands

```bash
python codex_workflow.py list
python -m pytest -q tests --basetemp .tmp_pytest_task051_full
python scripts/check_env.py --dry-run
npm run build
```

## Result

- `python codex_workflow.py list`: passed after Windows UTF-8 stdout guard in `codex_workflow.py`
- full pytest: `38 passed in 0.70s`
- `npm run build`: passed
- `check_env.py --dry-run`: passes for the `.env.example` Ghost key format after updating the placeholder to a valid hex secret shape

## Notes

- Local `check_env.py --dry-run` still reports missing `langfuse` import in the current interpreter. CI installs `requirements.txt`, which already includes `langfuse`, so the workflow path remains aligned.
