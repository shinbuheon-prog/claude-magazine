# TASK_050 Smoke Report

Date: 2026-04-24

Commands:

```bash
python -m py_compile scripts/publish_monthly.py
python scripts/publish_monthly.py --month 2026-05 --status
python scripts/publish_monthly.py --month 2026-05 --from-stage pdf_compile --dry-run --yes
python scripts/export_metrics.py --since-days 30 --format json --output output/task050_metrics.json
python -m pytest -q tests/test_publish_monthly.py
```

Checks:

- `--status` prints current stage progress and last update timestamp
- `--from-stage` skips earlier stages and records telemetry for resumed stages
- Publish state JSON now includes `telemetry`
- Metrics export includes `operations.publish_monthly.last_month`
- Metrics export includes `stages_duration_sec` and `stages_cost_usd`

Notes:

- The smoke run updated `reports/publish_state_2026-05.json` and `reports/publish_2026-05.md`.
